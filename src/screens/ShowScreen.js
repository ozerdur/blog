import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const ShowScreen = ({ params }) => {
  return (
    <View>
      <Text>ShowScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ShowScreen;
